<h1>WELCOME TO THE PUBLIC PRIVATE
<p>We are private gigs here!</p>

<% if logged_in? %>
  <h2>You are loggen in, <%= current_user.name %></h2>
<% end %>

<%= link_to "Login", new_session_path %>
<%= link_to "Sign Up", new_user_path %>
